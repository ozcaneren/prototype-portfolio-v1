/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'picsum.photos', 'res.cloudinary.com', 'picsum.photos', 'cdn.discordapp.com', 'i.hizliresim.com'],
  },
}

module.exports = nextConfig
